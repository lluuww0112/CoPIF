defaults:
  - base
  - _self_

train:
  batch_size: 4
  num_epochs: 1
  num_workers: 0
  log_every: 1
  max_steps: 10
  checkpoint_dir: "history/test"
  save_steps: 2
  save_total_limit: 2
  warmup_steps: 0
